language: python

python:
  - "2.7"
  - "2.6"

cache:
  directories:
    - $HOME/.cache/pip

install:
  - pip install -r requirements.txt
  - pip install -r requirements-test.txt
  - python setup.py install

script: nosetests -v
